<Window x:Class="SemanticKernelSample.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SemanticKernelSample"
        mc:Ignorable="d"
        Title="MainWindow" Height="600" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <Grid >
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition />
                <RowDefinition  Height="Auto"/>
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0" Text="请输入你的提示词："  FontSize="20"/>
            <TextBox Grid.Row="1" x:Name="PromptTextBox"   Margin="5"/>
            <Button Grid.Row="2" x:Name="ExecuteButton" Width="80" Height="40" Margin="2" HorizontalAlignment="Right"  Content="执行" Click="ExecuteButton_OnClick" />
        </Grid>
        <TextBox Grid.Row="1" x:Name="ResponseTextBox"  TextWrapping="Wrap" Margin="5"/>
    </Grid>
</Window>
